name: Keep Render App Alive

on:
  schedule:
    - cron: "*/30 * * * *"  # every 10 minutes (UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          curl -fsS "${{ secrets.KEEPALIVE_URL }}" || true
